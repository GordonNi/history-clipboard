# 2026-06-23 开发日志

## 已完成

- 创建项目治理结构。
- 创建 `docs/` 文档目录。
- 创建 `dev-logs/` 开发日志目录。
- 编写产品需求文档。
- 编写技术架构规范。
- 编写 UI 与交互设计规范。
- 编写分阶段开发计划。
- 编写质量、安全与验收标准。
- 编写项目代理工作说明 `AGENTS.md`。
- 创建 Swift Package 形式的 macOS 基础项目。
- 创建基础 SwiftUI App 启动入口。
- 创建基础内容视图。
- 添加 `.gitignore`，忽略 `.DS_Store`、`.build/` 和 Xcode 用户状态文件。
- 更新技术文档和开发计划，明确当前工程可通过 Xcode 打开 `Package.swift`。
- 实现菜单栏应用骨架。
- 新增 `AppDelegate`，使用 `NSStatusItem` 创建菜单栏图标。
- 使用 `NSPopover` 承载 SwiftUI 基础面板。
- 将 App 切换为 accessory 模式，减少普通 Dock 窗口存在感。
- 将基础内容视图调整为菜单栏弹出面板占位 UI。
- 实现文字剪贴板监听。
- 新增临时内存历史数据模型和数据仓库。
- 新增 `ClipboardMonitor`，通过 `NSPasteboard.general.changeCount` 轮询系统剪贴板变化。
- 将复制到剪贴板的文字按时间倒序插入历史列表。
- 连续复制相同文字时不重复新增。
- 将菜单栏弹出面板从空状态升级为可显示临时文字历史列表。
- 实现文字历史本地持久化。
- 新增 Core Data 持久化控制器，使用代码内数据模型。
- 将文字历史保存到 `~/Library/Application Support/HistoryClipboard/HistoryClipboard.sqlite`。
- 应用启动时从本地数据库读取已有文字历史。
- 为后续阶段预留单条删除的数据层方法。
- 实现历史列表基础操作。
- 点击文字历史卡片后，将该文字重新复制到系统剪贴板。
- 重新复制历史卡片时同步更新监听器的剪贴板变更计数，避免应用自己的复制动作被误记为新历史。
- 在文字卡片右上角增加单条删除按钮。
- 删除单条历史时同步更新列表和本地 Core Data 数据。
- 点击复制后显示短暂“已复制”反馈。
- 实现搜索与置顶。
- 搜索框从占位状态改为可输入，并按文字内容实时过滤历史。
- 搜索无结果时显示独立空状态。
- 为文字历史增加 `isPinned` 字段。
- 置顶内容显示在普通内容前面，置顶组内部仍按时间降序排列。
- 在文字卡片中增加置顶和取消置顶按钮。
- 置顶状态同步保存到 Core Data。
- Core Data 持久化启用自动轻量迁移，以兼容新增置顶字段。
- 优化文字历史卡片交互：点击整张卡片框即可重新复制，不再只限于点击正文文字。
- 将主界面升级为半透明液态玻璃质感。
- 新增 `VisualEffectView`，封装 macOS 原生 `NSVisualEffectView` 毛玻璃材质。
- 面板背景、搜索框、历史卡片和底部栏改为半透明玻璃层。
- 保留淡蓝色作为低透明度叠色，并保持文字可读性。
- 更新 UI 与交互设计规范，记录玻璃质感标准。
- 实现保存期限设置。
- 新增 `AppSettingsStore`，使用 `UserDefaults` 保存 1 天、3 天、5 天选项。
- 默认保存期限为 3 天。
- 在底部栏加入保存期限分段选择。
- 增加过期未置顶历史清理逻辑。
- 清理会在应用启动、保存期限变更、复制新增后触发。
- 置顶内容不受保存期限影响。
- 实现图片剪贴板支持。
- 新增 `ClipboardImageStore`，将图片保存为 PNG 文件。
- 图片文件保存到 `~/Library/Application Support/HistoryClipboard/Images/`。
- Core Data 增加 `imagePath` 字段，并继续使用自动轻量迁移。
- 剪贴板监听在无纯文本时读取图片对象并记录为图片历史。
- 历史列表支持显示图片缩略图。
- 点击图片历史卡片可将图片重新复制到系统剪贴板。
- 删除图片历史和过期清理图片历史时会同步删除本地图片文件。
- 增加图片缩略图悬停预览效果：鼠标放在图片上时适度放大并增加阴影，不改变列表布局尺寸。
- 实现设置与隐私控制。
- 增加暂停记录开关，开启后剪贴板变化不会新增历史。
- 增加清空全部历史按钮，并提供二次确认弹窗。
- 清空全部历史会删除 Core Data 记录和关联本地图片文件。
- 增加开机启动开关，使用 macOS `ServiceManagement` 接入。
- 当前命令行调试运行形态可能无法完成开机启动注册，失败时会显示提示；正式 App bundle 中需要最终手动验证。
- 完成 UI 打磨与稳定性检查阶段。
- 更新空状态文案，不再使用“临时”描述，明确支持文字和图片。
- 底部统计改为区分文字数量和图片数量。
- 暂停记录开启时，空状态中显示暂停提示。
- 底部设置区图标和文案优化，提升半透明玻璃界面可读性。
- 新增 `docs/manual-acceptance.md`，记录第一版手动验收清单。
- 修复图片卡片置顶/删除不易点击的问题。
- 将整张卡片级复制手势改为内容区复制手势，避免和右上角置顶/删除按钮抢点击事件。

## 待办

- 按 `docs/manual-acceptance.md` 完成完整手动验收。
- 正式打包为 macOS App 后，最终验证开机启动开关。
- 后续可按验收反馈继续修复或优化。

## 问题

- 暂无阻塞问题。

## 下一步

- 按 `docs/manual-acceptance.md` 手动检查第一版主流程。
- 如果验收通过，再考虑正式打包和发布准备。

## 验证记录

- 已检查项目文件夹初始为空。
- 已创建项目治理文件。
- 已复核 `docs/`、`dev-logs/` 和 `AGENTS.md` 文件结构。
- `swift build` 首次在沙盒中因 SwiftPM/Clang 用户缓存权限失败，随后按规则使用提升权限重试。
- `swift build` 构建通过。
- `.build/debug/HistoryClipboard` 已生成为 Mach-O 64-bit arm64 可执行文件。
- 已短暂启动 `.build/debug/HistoryClipboard` 并在 2 秒后主动结束进程；退出码 143 来自验证脚本的主动终止。
- 阶段 3 后再次执行 `swift build`，构建通过。
- 已短暂启动菜单栏骨架应用并在 3 秒后主动结束进程；退出码 143 来自验证脚本的主动终止。
- 启动结束时出现 macOS GUI/XPC 连接提示，发生在脚本主动终止进程时，未阻塞构建或启动验证。
- 阶段 4 后再次执行 `swift build`，构建通过。
- 已短暂启动应用并通过 `pbcopy` 写入验证文字，应用保持运行直到脚本主动结束。
- 阶段 4 的历史记录仍为临时内存数据，退出应用后会清空，这是本阶段预期行为。
- 阶段 5 后再次执行 `swift build`，构建通过。
- 已启动应用并通过系统剪贴板写入英文验证文本，确认 SQLite 中存在对应记录。
- 已通过 AppleScript 设置中文剪贴板文本，确认中文内容可写入并从 SQLite 查询到。
- 验证过程中写入的测试历史保留在本地开发数据库中，后续阶段加入删除功能后可从 UI 清理。
- 阶段 6 后再次执行 `swift build`，构建通过。
- 已短暂启动应用并加载本地历史数据，应用保持运行直到脚本主动结束。
- 点击卡片重新复制和单条删除属于菜单栏交互，已完成代码路径接入，建议手动点击面板进行最终确认。
- 阶段 7 后再次执行 `swift build`，构建通过。
- 已启动应用触发 Core Data 轻量迁移，SQLite 表中已出现 `ZISPINNED` 字段。
- 搜索输入、置顶和取消置顶属于菜单栏 UI 交互，已完成代码路径接入，建议手动点击面板进行最终确认。
- 调整卡片点击区域后再次执行 `swift build`，构建通过。
- 增加液态玻璃视觉风格后再次执行 `swift build`，构建通过。
- 已短暂启动应用，确认新增毛玻璃视图不影响启动。
- 阶段 8 后再次执行 `swift build`，构建通过。
- 已向本机开发数据库插入唯一过期置顶/未置顶测试记录并启动应用验证：未置顶记录被自动清理，置顶记录被保留。
- 验证结束后已删除保存期限测试标记，确认无 `retention-test-%` 记录残留。
- 阶段 9 后再次执行 `swift build`，构建通过。
- 已启动应用并向系统剪贴板写入临时生成的小图片，确认数据库新增图片记录且本地 PNG 文件存在。
- 验证结束后已删除图片测试记录和对应 PNG 文件，确认无图片测试记录残留。
- 增加图片悬停预览效果后再次执行 `swift build`，构建通过。
- 阶段 10 后再次执行 `swift build`，构建通过。
- 已短暂启动应用，确认新增设置项和 `ServiceManagement` 状态读取不影响启动。
- 清空全部、暂停记录和开机启动属于菜单栏 UI 交互，建议手动点击面板进行最终确认。
- 阶段 11 后再次执行 `swift build`，构建通过。
- 已短暂启动应用，确认 UI 打磨后仍可正常启动。
- 已补充第一版手动验收清单 `docs/manual-acceptance.md`。
- 修复图片卡片操作按钮点击冲突后再次执行 `swift build`，构建通过。
