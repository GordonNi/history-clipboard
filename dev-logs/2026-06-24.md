# 2026-06-24 开发日志

## 已完成

- 读取 `AGENTS.md`、手动验收清单和上一日开发日志，确认第一版功能开发已完成。
- 确认下一步为正式打包与验收准备。
- 新增 `scripts/package-app.sh` 打包脚本。
- 使用 release 构建生成 `dist/HistoryClipboard.app`。
- App 包包含标准 `Contents/Info.plist` 和 `Contents/MacOS/HistoryClipboard`。
- `Info.plist` 设置 `LSUIElement`，应用以菜单栏工具方式运行，不显示 Dock 图标。
- 新增 `docs/packaging.md`，记录打包、运行、数据位置和注意事项。
- 更新 `AGENTS.md`，加入打包说明文档路径。
- 使用 imagegen 生成淡蓝色 iOS 液态玻璃风格 App 封面图。
- 新增 `Assets/AppIcon/app-cover-liquid-glass.png` 原始封面资产。
- 新增 `Assets/AppIcon.iconset/` 多尺寸图标集。
- 生成 `Assets/AppIcon/HistoryClipboard.icns` macOS 应用图标文件。
- 更新 `scripts/package-app.sh`，打包时自动复制应用图标到 `.app/Contents/Resources/`。
- 更新 `Info.plist` 生成逻辑，写入 `CFBundleIconFile`。
- 更新 `docs/design-guidelines.md` 和 `docs/packaging.md`，记录 App 图标设计与打包规则。
- 用户确认完整手动验收未发现问题。
- 新增 `scripts/install-local.sh` 本机安装脚本。
- 更新 `docs/development-plan.md`，加入阶段 12：本地安装准备。
- 更新 `docs/packaging.md`，加入本机安装、启动和自定义安装目录说明。
- 更新 `AGENTS.md`，记录下一阶段为本地安装准备。

## 待办

- 如验收通过，可考虑签名、公证和安装分发方案。
- 如果 Finder 中旧图标缓存未立即刷新，后续验收时可重新打开 Finder 或重新复制 App 包确认。
- 在用户真实 `~/Applications` 目录执行一次本机安装。
- 在安装后的 App 中最终确认开机启动开关。
- 如需要给其他电脑使用，下一步准备签名、公证和压缩分发方案。

## 问题

- `iconutil` 在沙盒环境下会对有效 iconset 报 `Invalid Iconset`，已通过非沙盒命令生成本地图标文件。

## 下一步

- 运行 `open dist/HistoryClipboard.app`。
- 按 `docs/manual-acceptance.md` 检查第一版主流程。
- 在 Finder 中确认 `dist/HistoryClipboard.app` 展示新液态玻璃图标。
- 运行 `scripts/install-local.sh` 安装到 `~/Applications`。

## 验证记录

- 已确认项目当前阶段为第一版功能开发完成。
- `scripts/package-app.sh` 执行成功。
- release 构建通过。
- 已检查 `dist/HistoryClipboard.app` 文件结构。
- 已检查 `Info.plist`，确认 bundle id、展示名称、最低系统版本和 `LSUIElement` 配置存在。
- 已确认可执行文件为 arm64 Mach-O。
- 已通过 `open dist/HistoryClipboard.app` 短暂启动打包后的 App，并主动结束进程。
- 已确认 `Assets/AppIcon/HistoryClipboard.icns` 成功生成。
- 已重新运行 `scripts/package-app.sh`，release 构建通过。
- 已确认 `dist/HistoryClipboard.app/Contents/Resources/HistoryClipboard.icns` 存在。
- 已确认 `Info.plist` 包含 `CFBundleIconFile = HistoryClipboard`。
- 已通过 `open dist/HistoryClipboard.app` 再次短暂启动打包后的 App，并主动结束进程。
- 已重新运行 `scripts/package-app.sh`，release 构建通过。
- 已使用 `INSTALL_DIR=/private/tmp/HistoryClipboardInstallTest scripts/install-local.sh` 完成临时安装验证。
- 已确认临时安装后的 App 包存在。
- 已确认临时安装后的 `Info.plist` 保留展示名称、图标字段和 `LSUIElement` 配置。
